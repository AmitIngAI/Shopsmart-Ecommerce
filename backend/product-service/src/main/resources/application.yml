spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: mongodb+srv://shopmart_01:XiaCTF3cUNbmbyDc@cluster1.a8kvqvy.mongodb.net/productdb?retryWrites=true&w=majority
      database: productdb

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}

logging:
  level:
    org.springframework.data.mongodb: DEBUG